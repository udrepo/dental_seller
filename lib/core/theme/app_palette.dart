import 'package:flutter/material.dart';

class AppPalette {
  // Base Colors
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color gray = Color(0xFFE0E0E0);

  // Brand Colors
  static const Color primary = Color(0xFF44BDC5);       // Teal-ish
  static const Color secondary = Color(0xFFA1D1E7);     // Light Blue
  static const Color secondaryDark = Color(0xFF6D8CAB); // Muted Blue
}